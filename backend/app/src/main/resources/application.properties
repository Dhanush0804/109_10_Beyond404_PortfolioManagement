spring.application.name=app

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/beyond404}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:n3u3da!}

# External Market Data API
market.data.base-url=http://localhost:8000

# Currency Conversion API
currency.api.base-url=https://api.frankfurter.app

# Execute schema.sql and data.sql
spring.sql.init.mode=always
spring.sql.init.continue-on-error=false
spring.sql.init.encoding=UTF-8
